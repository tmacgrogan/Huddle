run setup.bat for initial setup

execute "node serve.js" to run the server
navigate to http://localhost:8080 in your browser
POST http://localhost:8080/create will create a huddle
GET http://localhost:8080/timeline will get the timeline

use the Postman Chrome extension or something similar to build post requests in your browser.
something is going wrong with the json or the database when you request either of the functions, 
	and im too tired to figure it out right now. see if you can figure anything out and ill take another look when i wake up
	
